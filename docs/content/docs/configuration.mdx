---
title: Configuration
description: All createKavach() options explained.
icon: Settings
---

import { Callout } from 'fumadocs-ui/components/callout';
import { TypeTable } from 'fumadocs-ui/components/type-table';
import { Card, Cards } from 'fumadocs-ui/components/card';

`createKavach()` accepts a `KavachConfig` object. The only required field is `database`. Everything else is optional and enables features incrementally.

```typescript
import { createKavach } from 'kavachos';

const kavach = await createKavach({
  database: { provider: 'sqlite', url: 'kavach.db' },
});
```

## Top-level options

<TypeTable
  type={{
    database: {
      description: 'Database connection config. Required.',
      type: 'DatabaseConfig',
      required: true,
    },
    agents: {
      description: 'Agent identity settings.',
      type: 'AgentConfig',
    },
    mcp: {
      description: 'MCP OAuth 2.1 authorization server.',
      type: 'McpConfig',
    },
    auth: {
      description: 'Human auth adapter and session config.',
      type: '{ adapter?: AuthAdapter; session?: SessionConfig }',
    },
    anomaly: {
      description: 'Anomaly detection thresholds.',
      type: 'AnomalyConfig',
    },
    approval: {
      description: 'CIBA async approval flow config.',
      type: 'ApprovalConfig',
    },
    trust: {
      description: 'Graduated autonomy trust scoring.',
      type: 'TrustConfig',
    },
    telemetry: {
      description: 'OpenTelemetry integration via hooks.',
      type: 'TelemetryConfig',
    },
    hooks: {
      description: 'Lifecycle hooks for sandboxing and custom validation.',
      type: 'KavachHooks',
    },
    baseUrl: {
      description: 'Base URL for the auth server (e.g. https://auth.example.com).',
      type: 'string',
    },
    secret: {
      description: 'Secret key used to sign tokens. Min 32 characters.',
      type: 'string',
    },
  }}
/>

## Database config

<TypeTable
  type={{
    provider: {
      description: 'Database driver to use.',
      type: '"sqlite" | "postgres" | "mysql"',
      required: true,
    },
    url: {
      description: 'File path for SQLite, connection string for Postgres/MySQL.',
      type: 'string',
      required: true,
    },
    skipMigrations: {
      description:
        'Skip automatic CREATE TABLE IF NOT EXISTS on init. Use when you manage migrations externally (Flyway, drizzle-kit push, etc.). Defaults to false.',
      type: 'boolean',
    },
  }}
/>

```typescript
// SQLite
database: { provider: 'sqlite', url: './kavach.db' }

// Postgres
database: { provider: 'postgres', url: process.env.DATABASE_URL }

// MySQL
database: { provider: 'mysql', url: process.env.DATABASE_URL }

// In-memory SQLite (tests)
database: { provider: 'sqlite', url: ':memory:' }
```

## Agent config

Controls the agent identity lifecycle.

<TypeTable
  type={{
    enabled: {
      description: 'Enable agent identity management.',
      type: 'boolean',
      required: true,
    },
    maxPerUser: {
      description: 'Maximum number of agents a single user can create.',
      type: 'number',
    },
    defaultPermissions: {
      description: 'Permission strings assigned to every new agent unless overridden.',
      type: 'string[]',
    },
    auditAll: {
      description: 'Write every agent action to the audit log regardless of permission outcome.',
      type: 'boolean',
    },
    tokenExpiry: {
      description: 'Default token expiry duration string (e.g. "7d", "24h", "30m").',
      type: 'string',
    },
  }}
/>

```typescript
agents: {
  enabled: true,
  maxPerUser: 10,
  defaultPermissions: [],
  auditAll: true,
  tokenExpiry: '30d',
},
```

## MCP config

Enables the OAuth 2.1 authorization server for MCP-compliant tool access.

<TypeTable
  type={{
    enabled: {
      description: 'Enable the MCP authorization server.',
      type: 'boolean',
      required: true,
    },
    issuer: {
      description: 'Token issuer URL. Appears as the iss claim in JWTs.',
      type: 'string',
    },
    baseUrl: {
      description: 'Base path for MCP endpoints.',
      type: 'string',
    },
    signingSecret: {
      description: 'Secret used to sign JWTs. Min 32 characters. Defaults to the top-level secret.',
      type: 'string',
    },
    accessTokenTtl: {
      description: 'Access token lifetime in seconds. Defaults to 3600 (1 hour).',
      type: 'number',
    },
    refreshTokenTtl: {
      description: 'Refresh token lifetime in seconds. Defaults to 604800 (7 days).',
      type: 'number',
    },
    codeTtl: {
      description: 'Authorization code lifetime in seconds. Defaults to 600 (10 minutes).',
      type: 'number',
    },
    enforceAuth: {
      description: 'Reject all MCP requests without a valid Bearer token.',
      type: 'boolean',
    },
    scopes: {
      description: 'Custom OAuth scopes supported by this server.',
      type: 'string[]',
    },
    allowedResources: {
      description: 'Allowed resource URIs for RFC 8707 resource indicators.',
      type: 'string[]',
    },
    loginPage: {
      description: 'URL of your login page. Users are redirected here when unauthenticated.',
      type: 'string',
    },
    consentPage: {
      description: 'URL of your consent page. Users are redirected here to approve scopes.',
      type: 'string',
    },
    preRegisteredClients: {
      description: 'OAuth clients registered at startup (first-party apps, CLIs, test fixtures).',
      type: 'Array<{ clientId, clientSecret?, redirectUris, clientName?, scope? }>',
    },
    getAdditionalClaims: {
      description: 'Async function to add custom claims to issued tokens.',
      type: '(userId: string, scopes: string[]) => Promise<Record<string, unknown>>',
    },
  }}
/>

```typescript
mcp: {
  enabled: true,
  issuer: 'https://auth.example.com',
  baseUrl: 'https://auth.example.com',
  accessTokenTtl: 3600,
  refreshTokenTtl: 604800,
  enforceAuth: true,
  loginPage: 'https://example.com/login',
  consentPage: 'https://example.com/consent',
},
```

## Auth config

Connects KavachOS to your existing auth provider so it can resolve the human user behind incoming requests.

```typescript
auth: {
  adapter: betterAuthAdapter(auth),   // resolves user from request
  session: {                          // optional: KavachOS-managed sessions
    secret: process.env.SESSION_SECRET,
    maxAge: 60 * 60 * 24 * 30,       // 30 days in seconds
  },
},
```

When `auth` is omitted, `kavach.auth.resolveUser()` always returns `null` (manual user management mode).

See [Auth adapters](/docs/guides/auth-adapters) for all adapter options.

## Anomaly config

<TypeTable
  type={{
    highFrequencyThreshold: {
      description: 'Calls per agent per hour before flagging as high-frequency. Defaults to 500.',
      type: 'number',
    },
    highDenialRateThreshold: {
      description: 'Denial rate percentage that triggers an alert. Defaults to 50.',
      type: 'number',
    },
    expectedHours: {
      description:
        'Flag access outside these hours (0-23) as off-hours anomaly. Optional.',
      type: '{ start: number; end: number }',
    },
  }}
/>

```typescript
anomaly: {
  highFrequencyThreshold: 200,
  highDenialRateThreshold: 30,
  expectedHours: { start: 8, end: 20 },
},
```

## Environment variables pattern

Never hardcode secrets in config. Pass them through environment variables:

```typescript
const kavach = await createKavach({
  database: {
    provider: 'postgres',
    url: process.env.DATABASE_URL!,
  },
  secret: process.env.KAVACH_SECRET!,
  mcp: {
    enabled: true,
    issuer: process.env.KAVACH_ISSUER!,
    signingSecret: process.env.KAVACH_SIGNING_SECRET,
  },
});
```

## Dev vs production example

```typescript
// config/kavach.ts
const isDev = process.env.NODE_ENV !== 'production';

export const kavach = await createKavach({
  database: isDev
    ? { provider: 'sqlite', url: './kavach-dev.db' }
    : { provider: 'postgres', url: process.env.DATABASE_URL! },

  secret: process.env.KAVACH_SECRET!,
  baseUrl: process.env.KAVACH_BASE_URL ?? 'http://localhost:3000',

  agents: {
    enabled: true,
    maxPerUser: isDev ? 100 : 25,
    auditAll: true,
    tokenExpiry: '30d',
  },

  mcp: {
    enabled: true,
    issuer: process.env.KAVACH_BASE_URL ?? 'http://localhost:3000',
    accessTokenTtl: isDev ? 86400 : 3600,
    enforceAuth: !isDev,
    loginPage: '/login',
    consentPage: '/consent',
  },

  anomaly: {
    highFrequencyThreshold: 500,
    highDenialRateThreshold: 50,
  },
});
```

<Callout type="warn">
  `secret` and `mcp.signingSecret` must be at least 32 characters. In production, generate them with `openssl rand -base64 32`.
</Callout>

## Next steps

<Cards>
  <Card title="Auth adapters" description="Connect to better-auth, Auth.js, or Clerk." href="/docs/auth-adapters" />
  <Card title="Database setup" description="SQLite, Postgres, or MySQL configuration." href="/docs/database" />
  <Card title="Framework adapters" description="Mount KavachOS on your framework." href="/docs/adapters" />
</Cards>
